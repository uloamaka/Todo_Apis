// const swaggerUi = require("swagger-ui-express");
// const swaggerSpec = "../../swagger_output.json";

// const { serve } = swaggerUi;
// const setup = swaggerUi.setup(swaggerSpec);

// module.exports = {
//   serve,
//   setup,
// };
